// Iterables 

// Seq, Set, Map

// Seq can be optimized for indexing (Vector) or linear seq (List)


// Core methods: map, flatMap, filter, foldLeft, foldRight

// Simple for

for (x <- range) yield x

for (x <- e1; y <- e2; s) yield e3

